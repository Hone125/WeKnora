# 课题三验收整改记录（2026-09-11）

> **2026-09-12 状态更新：核心验收项已全部拿到云端 CI 直接计数证据。** ① CI 拦截降召回——run 34684475268 真实改坏检索阈值后门禁 `passed=false`、6 指标报回归；② embedding 缓存降幅——run 34684838421 冷 36 次真实 HTTP → 热 0 次、DB 命中 60；③ 冷缓存直接计数——run 34681726847；④ 页面数据库用量——表名已修、单测覆盖 SQL 表名/租户/模型/时间过滤，真实调用链账本由 `scripts/usage-check.mjs` 查 `/api/v1/models/usage` 在 CI 验证。Tag `rhino-2026-final-7` 已指向最终提交 518f6577 并推送。下方 9-11 的历史记录保留为整改过程。

依据：课题 PDF 第 4–5 页及流程指引全文。截止为北京时间 2026-09-13 00:00；建议提前半天提交。已发布 Tag 不得覆盖或删除。获奖由评审决定，不承诺奖项。

| 验收 | 当前边界 | 优先工作 |
|---|---|---|
| 干净环境一条命令复现 | ✅ CI 里从零起 postgres+redis+server 自动跑通评测（topic3-eval-real.yml / cache-hit.yml），一条命令复现 | 已完成 |
| CI 拦截降召回改动 | ✅ 真实 CI 跑通：门禁真实拦截退化（run 34684475268，6 指标报回归） | 已完成 |
| 页面数据库用量 | 表名已修，单测覆盖 SQL 表名/租户/模型/时间过滤；真实调用链账本由 usage-check.mjs + CI 验证 | 核验真实 DB 记录 |
| embedding 调用降幅 | ✅ 云端 CI 正式调用链直接计数：冷 36 次 HTTP → 热 0 次、DB 命中 60（run 34684838421） | 已完成 |
| Wiki 缓存提升 | 前缀字节比例不是命中率 | ✅ 已测厂商实际缓存 token：重排后命中率 98.2% vs 对照 0% |
| 8 解析器（选做） | 部分引擎、格式差异影响分数 | 优先规范已有实验 |

口径：结果存于 evaluation_tasks；30 个文本不等于 30 次 HTTP 请求；丢弃内存对象不等于实际重启；当前价格是估算而非历史账单；单个 DOCX 异常不能推广为引擎普遍缺陷。HTML/Markdown 需统一后评分。

本轮先修核心故障并增加直接覆盖正式代码的回归测试。未执行的验证不标记通过。暂不推送、打 Tag 或发邮件。

## 已实施

- 为 ModelUsageRecord 指定迁移中的 model_usages 表名。
- 并发装饰器保留外层缓存回调；WeKnoraCloud 批量入口也尊重传入包装器。
- 门禁缺失指标、缺基线、无效数值、失败/运行中任务不再放行。CLI 区分退化（1）与数据错误（2）。
- 新增 CLI 回归用例、实际包装链缓存用例、GORM SQL 表名与租户/模型/时间过滤用例。
- CI 健康样例改为完整基线；这仍只是门禁逻辑测试，不冒充真实检索评测。
- Wiki 固定前置重排已用厂商真实缓存 token 实测（`cmd/wikicachebench`，SiliconFlow DeepSeek-V3）：重排后命中率 98.2%（中位数），对照（动态前置）0%。详见 M3②。
- 真实评测接入 CI：`.github/workflows/topic3-eval-real.yml` 在 CI 起最小后端（postgres 带 pgvector + redis + 编译启动 server），用真实模型 Key 跑 30 题评测再走门禁；编排脚本 `scripts/ci-eval.sh` 负责等健康、注册临时账号、跑评测。详见 rhino-topic3-real-eval-ci.md。

## 验证状态

- `go test ./internal/evalgate ./cmd/evalgate ./internal/parsequality` 已通过（2026-09-11）。CLI 覆盖成功、退化、缺失/null/重复指标、未成功任务及非法数值。
- 正式缓存包装链、批量分发、并发控制及账本 SQL 回归测试已通过。首次下载超时后，使用可访问镜像和本地代理恢复。
- 真实 SQLite 测试已通过：用项目迁移建文件数据库、调用正式仓储写入、关闭并重新打开数据库，验证租户/模型/时间过滤及缓存命中率聚合。该测试不是服务器进程重启实验，也不是 PostgreSQL 验证。
- Docker 初始状态未运行；已尝试启动但尚未确认服务就绪，PostgreSQL 和服务器重启实验尚未完成。
- 新增 topic3-contracts.yml 自动运行正式缓存和账本回归测试；本地通过，远端工作流尚未执行。这不等于已接入真实检索召回门禁。
- 真实评测 CI（topic3-eval-real.yml）：已从源码逐层确认最小拓扑（纯文本评测不依赖 docreader）、模型自动注册（builtin_models.yaml）、注册/登录/评测/门禁接口与退出码；workflow YAML 与编排脚本语法已本地校验通过。端到端真实跑通需在配好 Secrets 的 fork 上触发一次，本机无 Docker/PostgreSQL 无法离线复现。
- `git diff --check` 通过。未推送、未创建或覆盖 Tag。

## 截止前执行顺序

### 第二轮优化（2026-09-11）

- 两个 shell 入口统一调用 scripts/eval-runner.mjs；去掉固定示例密码，增加 HTTP/全流程超时和严格成绩校验。失败退出 2，退化退出 1。
- 自动保存结果、指纹、客户端版本和失败摘要；明确不保证客户端与服务器版本一致。默认不提交含评测内容的证据目录。
- 模型用量区间统一为 [start, end)，前端按本地日期换算，旧响应不覆盖新筛选。未知单价与接口错误不再伪装成零费用/空记录。
- 真实 SQLite 时间边界回归通过；前端 npm run type-check 通过。
- 评测 HTTP 客户端 10 项测试全部通过（包含证据保存、凭据字段过滤及调用真实 Go 门禁返回退化退出码）。以上 HTTP 服务是本机模拟，不代表真实检索评测已接入 CI。
- 操作说明：rhino-topic3-evaluation-runner.md。Docker 引擎仍未就绪，PostgreSQL、真实索引及 Wiki 实验未完成。

1. P0：正式缓存、账本回归测试通过，核实迁移和页面数据。
2. P0：固定评测配置与原始结果、失败题清单；清洁环境复现，并把实际检索任务接到 CI。
3. P0：实际模型请求的冷/热/重启缓存实验，分别记录请求数与文本数。
4. P1：✅ Wiki 重排前后厂商缓存 token 对照已完成（`cmd/wikicachebench`，SiliconFlow DeepSeek-V3：重排后命中率 98.2% 中位数、对照 0%）；供应商未返回缓存字段时工具会标记「无法测量」。
5. P1：修正报告全部证据口径，检查最终代码与提交材料一致。已发布 Tag 不覆盖，修订提交版本需按导师指引处理。
6. P2：格式归一化后补解析基线；不为选做项挤占核心验收时间。
